import os
import json
import pandas as pd

def tweets_analysis(path, tags=[], bin_frequency="600s"):
    pass


def predict_tweet_counts(tag, time):
    """Optional. Feel free to change the header if additional arguments are needed"""
    pass